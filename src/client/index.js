// Libraries
import React from 'react';
import ReactDOM from 'react-dom';

// Components
import NestingDropTargets from './DropTargets/';

const mountNode = document.getElementById('app');
ReactDOM.render(<NestingDropTargets />, mountNode);
